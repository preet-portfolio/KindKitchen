// CameraView.swift
import SwiftUI

struct CameraView: View {
    var body: some View {
        Text("Camera Screen")
            .font(.title)
            .padding()
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}
