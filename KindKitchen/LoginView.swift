// LoginView.swift
import SwiftUI

struct LoginView: View {
    var body: some View {
        Text("Login Screen")
            .font(.title)
            .padding()
    }
}

struct LoginView_Previews: PreviewProvider {
    static var previews: some View {
        LoginView()
    }
}
